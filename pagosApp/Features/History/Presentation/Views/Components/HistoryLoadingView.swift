import SwiftUI

struct HistoryLoadingView: View {
    var body: some View {
        VStack {
            Spacer()
            ProgressView("Cargando historial...")
            Spacer()
        }
    }
}
